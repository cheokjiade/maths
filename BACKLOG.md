# Backlog

Ideas and fixes to pick up later. Not yet started.

_(empty)_

## Done

- ~~**numbers20 "Order the numbers": drag-and-drop.**~~ Done — built as a shared
  `WS.enableDragOrder()` (tap-to-place + pointer drag, mouse/touch, no libraries, `file://`-safe),
  auto-marked by `WS.mark`, applied to the "Order the numbers" sections of `numbers20.html` and
  `numbers100.html`.
