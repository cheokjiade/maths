# Backlog

Ideas and fixes to pick up later. Not yet started.

- **numbers20.html — "Order the numbers": make it drag-and-drop.** Instead of typing the numbers
  into the boxes, let the child **drag** the given number tiles into the answer boxes below (and back
  out). Should still auto-mark (compare the dropped order to the sorted order), still work on touch
  (pointer events), still print as plain empty boxes, and stay `file://`-safe (no libraries / classic
  script). The same interaction would suit `numbers10.html` "what comes next" / future ordering
  exercises, so consider a small shared `WS` drag helper rather than a one-off.
